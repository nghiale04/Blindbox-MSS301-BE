

# ==========================
# APPLICATION INFO
# ==========================
spring.application.name=MSBlindBox_SE181765
server.port=8082
# ==========================
# DATABASE CONFIG
# ==========================
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:sqlserver://localhost:1433;databaseName=MSS301Summer25DBBlindBox;encrypt=true;trustServerCertificate=true}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sa}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:12345}

spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
# ==========================
# JWT CONFIG
# ==========================
app.jwt.secret=${APP_JWT_SECRET:NghiaLHSE181765xm8aylVXZ3sZjW+M+pL6fZdhvI40NMGvHhZBsR2YAwY2Z4Ktn}
app.jwt.access-ttl-seconds=900

# ==========================
# CORS CONFIG
# ==========================
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000,http://localhost:9090,http://172.237.80.224:9090,http://109.123.238.244:9090,http://172.31.48.1:5173}

# ==========================
# LOGGING
# ==========================
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.http.converter.json=DEBUG

# ==========================
# GATEWAY
# ==========================
spring.cloud.compatibility-verifier.enabled=true

# ==========================
# APP CUSTOM CONFIG
# ==========================
app.gateway-url=${APP_GATEWAY_URL:http://localhost:9090}
app.service-path=${APP_SERVICE_PATH:/blindbox-service}

# ==========================
# TIMEZONE (?? log & DB ??ng b? múi gi? Vi?t Nam)
# ==========================
spring.jackson.time-zone=Asia/Ho_Chi_Minh
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh

# ==========================
# BRAND SERVICE (Feign)
# ==========================
# You can override APP_BRAND_SERVICE_PATH via environment; by default it calls Brand service through API Gateway
app.brand-service-path=${APP_BRAND_SERVICE_PATH:${app.gateway-url}/brand-service}
